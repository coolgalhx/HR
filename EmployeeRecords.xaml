<Window x:Class="HR_Management_System.EmployeeRecords"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Employee Records"
        Height="600"
        Width="1050"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Text="Employee Records"
                   FontSize="22"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <!-- Search -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="txtsearchbar"
                     Width="300"
                     Height="28"
                     Margin="0,0,8,0"/>

            <Button x:Name="btngo"
                    Content="Go"
                    Width="80"
                    Height="28"
                    Click="btnGo_Click"/>
        </StackPanel>

        <!-- Main area -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Employee fields -->
            <StackPanel Grid.Column="0">
                <TextBlock Text="First Name"/>
                <TextBox x:Name="txtfirstname"/>

                <TextBlock Text="Last Name" Margin="0,6,0,0"/>
                <TextBox x:Name="txtlastname"/>

                <TextBlock Text="Employee ID" Margin="0,6,0,0"/>
                <TextBox x:Name="txtemployeeidERpg"/>

                <TextBlock Text="Date of Birth" Margin="0,6,0,0"/>
                <TextBox x:Name="txtdob"/>

                <TextBlock Text="Job Title" Margin="0,6,0,0"/>
                <TextBox x:Name="txtjobtitle"/>

                <TextBlock Text="Department" Margin="0,6,0,0"/>
                <TextBox x:Name="txtdepartment"/>

                <TextBlock Text="Employment Type" Margin="0,6,0,0"/>
                <TextBox x:Name="txtemploymenttype"/>

                <TextBlock Text="City" Margin="0,6,0,0"/>
                <TextBox x:Name="txtcity"/>

                <TextBlock Text="Address" Margin="0,6,0,0"/>
                <TextBox x:Name="txtaddress"/>

                <TextBlock Text="NI Number" Margin="0,6,0,0"/>
                <TextBox x:Name="txtnationalinsurance"/>

                <TextBlock Text="Contact Information" Margin="0,6,0,0"/>
                <TextBox x:Name="txtcontactinformation"/>

                <Button x:Name="btnEnter"
                        Content="Save / Update"
                        Height="30"
                        Margin="0,10,0,0"
                        Click="btnEnter_Click"/>
            </StackPanel>

            <!-- DataGrid -->
            <DataGrid Grid.Column="2"
                      x:Name="datagridloademployees"
                      AutoGenerateColumns="True"
                      CanUserAddRows="False"
                      IsReadOnly="True"/>
        </Grid>
    </Grid>
</Window>



<Window x:Class="HR_Management_System.LogIn"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HR_Management_System"
        mc:Ignorable="d" WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Title="LogIn" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="1.5"/>
        </Grid.ColumnDefinitions>

        <Border CornerRadius="10,0,0,10" Margin="6,-10,4,10">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#3AA9AD" Offset="0"/>
                    <GradientStop Color="#3AA9AD" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Button Content="Submit" Margin="438,202,206,160" Name="btnsubmit" Click="btnSubmit_Click"/>
        </Border>

        
        <TextBox TextWrapping="Wrap" Text=" " Margin="424,302,140,127" Grid.Column="0" Name="txtemployeeidLIpg"/>

        <Border BorderBrush="Black" BorderThickness="1" Grid.Column="1"/>
        <TextBox TextWrapping="Wrap" Text=" " Margin="424,351,140,78" Grid.Column="0" Name="txtpassword"/>
    </Grid>
</Window>
